version: v1

managed:
  enabled: true
  go_package_prefix:
    default: github.com/submaline/user-service/gen

plugins:
  - name: go
    out: gen
    opt: paths=source_relative
  - name: connect-go
    out: gen
    opt: paths=source_relative